{
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage --reporters=default --reporters=jest-html-reporter"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.405.0",
    "@aws-sdk/lib-dynamodb": "^3.405.0",
    "aws-sdk": "^2.1450.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "aws-sdk-client-mock": "^3.0.0",
    "jest": "^29.6.4",
    "jest-circus": "^29.6.4",
    "jest-html-reporter": "^3.10.2",
    "serverless-offline": "^12.0.4"
  }
}
